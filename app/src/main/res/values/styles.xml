<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">


        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/greenCode</item>

        <item name="colorAccent">@color/greenCode</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="sectionHeader" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:drawableBottom">@drawable/section_header</item>
        <item name="android:drawablePadding">4dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:paddingLeft">4dp</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomdialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#FFf17e4c</item>

    </style>

    <style name="navigationDrawercustomStyle">
        <item name="android:textSize">25sp</item>
        <item name="android:itemPadding">40dp</item>
        <item name="android:fontFamily">@font/lato</item>
        <item name="android:layout_marginTop">15dp</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
